@model MetroFlow.Models.ViewModels.TrainCreateEditViewModel
@{ ViewData["Title"] = Model.Id == 0 ? "Yeni Qatar" : "Qatar Düzəlt"; }
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card border-0 shadow-sm rounded-4">
                <div class="card-header bg-info text-white rounded-top-4 p-3">
                    <h5 class="mb-0"><i class="bi bi-train-front me-2"></i>@ViewData["Title"]</h5>
                </div>
                <div class="card-body p-4">
                    <form asp-action="@(Model.Id==0?"CreateTrain":"EditTrain")" method="post">
                        @Html.AntiForgeryToken()
                        <input asp-for="Id" type="hidden" />
                        <div asp-validation-summary="All" class="alert alert-danger py-2 small"></div>
                        <div class="mb-3">
                            <label asp-for="TrainNumber" class="form-label fw-semibold"></label>
                            <input asp-for="TrainNumber" class="form-control" placeholder="Q-001" />
                            <span asp-validation-for="TrainNumber" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Line" class="form-label fw-semibold"></label>
                            <select asp-for="Line" class="form-select">
                                <option value="Qırmızı">Qırmızı Xətt</option>
                                <option value="Yaşıl">Yaşıl Xətt</option>
                                <option value="Mavi">Mavi Xətt</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Capacity" class="form-label fw-semibold"></label>
                            <input asp-for="Capacity" type="number" class="form-control" />
                        </div>
                        <div class="form-check mb-4">
                            <input asp-for="IsActive" class="form-check-input" />
                            <label asp-for="IsActive" class="form-check-label">Aktiv</label>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-info text-white px-4"><i class="bi bi-save me-2"></i>Saxla</button>
                            <a asp-action="Trains" class="btn btn-outline-secondary">Ləğv Et</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
